     H
     FEMPPRT1   O    E             PRINTER OFLIND(*IN11)
     d empno           S              5s 0
     d empname         S             30a
     d depno           S              3a
     C     *IN03         DOWEQ     '0'
     C                   eval      FDATE = %DATE()
     C                   eval      FTIME= %TIME()
     C                   write     Header1
C    C                   write     header2
     C                   seton                                        03
     C                   ENDDO
     C     *IN11         IFeq      *ON
     C                   write     header1
     C                   write     header2
     C                   endif
       /free

          //Set SQL options
       exec sql
        SET OPTION commit=*none;



       //Read more than one record from the SQL table
       //Declare the cursor
         exec sql
        Declare C1 cursor for
       Select * from fakhir/empfile where depno = '900';
       //Open the cursor
         exec sql
          Open C1;

        //Fetch data in a loop
        Dou sqlcod <> 0;
          exec sql
          Fetch C1 INTO :empno , :empname,:depno ;
         If (sqlcod <> 0);
          Leave;
        Endif;
     C                   write     detail
        Enddo;

         //Close the cursor
         exec sql
          Close C1;


        /end-free
     C                   seton                                        LR
